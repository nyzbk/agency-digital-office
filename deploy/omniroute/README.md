# OmniRoute для Ultimatum Floor

Не второй репозиторий и не второй Vercel. Процесс 24/7 из того же штаба.

```bash
docker compose up -d
```

Дашборд: `http://VPS:20128`

Vercel `ultimatum-floor`:

```
OMNI_GATEWAY_URL=http://VPS:20128
OMNI_GATEWAY_KEY=<key>
```
