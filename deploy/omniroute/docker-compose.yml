services:
  omniroute:
    image: diegosouzapw/omniroute:latest
    container_name: ultimatum-omniroute
    restart: unless-stopped
    ports:
      - "20128:20128"
    volumes:
      - omni-data:/root/.omniroute
    environment:
      NODE_ENV: production

volumes:
  omni-data:
